<?php
  ///////////////////////////////////////
 // Change those to suite your needs ///
///////////////////////////////////////

$gravatar='on'; // gravatar support ( on | off )
$translateServ='0'; // translation feature ( 1 | 0 )
$typingServ='1'; // typing indicator feature ( 1 | 0 )
 $meImg=''; // if the gravatar is off define the link of the default avatar to use to urself (avatar should be 20*20px)
$senderImg=''; // if the gravatar is off define the link of the default avatar to use to the one who you talk with (avatar should be 20*20px)
$tzOffset=0; //(hours) GMT the default timeZone in the chat messages (8 => GMT+8 /  -6 => GMT-6)

  ////////////////////////////////////////////
 /// the following are the default values ///
////////////////////////////////////////////

$retryAfter=3;// the periode (seconds) to check for new messages recieved from whom you are chatting with
 $globalRetry=5;// the periode to check for new messages from whom you aren't chatting with
 $delUsers=15 ;// the period to delete a user if he shows no response (mode A only)
 $delMsgs=72;// the period to delete chat history (hours) 
$sessionVar='chatUser'; // the name of the session variable that stores the temporary id or name of the user when  (s)he tries to login
 date_default_timezone_set('UTC'); //set the default time zone to GMT, for more timezones check out http://www.php.net/timezones

?>